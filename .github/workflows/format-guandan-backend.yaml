name: Format Guandan backend (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-time formatter disabled after rustfmt commit"
